{% extends 'base.html.twig' %}

{% block title %}Competences{% endblock %}

{% block body %}
    <h1>Competences</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ competence.id }}</td>
            </tr>
            <tr>
                <th>Langage</th>
                <td>{{ competence.langage }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ competence.img }}</td>
            </tr>
            <tr>
                <th>Fontawesome</th>
                <td>{{ competence.fontawesome }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ competence.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('competences_index') }}">back to list</a>

    <a href="{{ path('competences_edit', {'id': competence.id}) }}">edit</a>

    {{ include('competences/_delete_form.html.twig') }}
{% endblock %}
